[package]
name = "epic"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.85"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "process", "fs", "signal", "io-util"] }
anyhow = "1"
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
clap = { version = "4", features = ["derive", "env"] }
ratatui = "0.29"
crossterm = "0.28"
lot = { git = "https://github.com/bitmonk8/lot", rev = "f131ad9", features = ["tokio"] }
tempfile = "3"
reel = { git = "https://github.com/bitmonk8/reel", rev = "93f35ef" }
vault = { git = "https://github.com/bitmonk8/vault", rev = "f7ecea1" }

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
nursery = { level = "deny", priority = -1 }
# Commonly disabled pedantic lints (too noisy / too opinionated)
missing_errors_doc = "allow"
missing_panics_doc = "allow"

module_name_repetitions = "allow"
must_use_candidate = "allow"

[dev-dependencies]
reel = { git = "https://github.com/bitmonk8/reel", rev = "93f35ef", features = ["testing"] }
