# U12-R4: cli + main / Dead code & cruft

## Summary
Both files are free of ZeroClaw/YAML/subprocess remnants and contain no TODO/FIXME markers. The main issues are a suppression lint annotation that masks extractable complexity and significant duplicated state-loading boilerplate across three code paths.

## Findings
| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | minor | `#[allow(clippy::too_many_lines)]` suppresses the lint rather than addressing it. `main()` is ~180 lines with three distinct phases (early-exit commands, state loading, orchestration). | `src/main.rs:26` | Extract state-loading logic into a helper (e.g., `load_or_create_state(cli, state_path) -> (EpicState, TaskId, String)`) and remove the allow annotation. |
| 2 | minor | Nearly identical state-load + root-id-check + error-message sequences are duplicated between the `Command::Run` branch (lines 66-95) and the `Command::Resume` branch (lines 125-153), and again in `print_status` (lines 214-227). Three copies of the same `EpicState::load` / `root_id()` / `get(root_id)` error cascade. | `src/main.rs:66-95`, `src/main.rs:125-153`, `src/main.rs:214-227` | Extract a shared helper, e.g., `fn load_state(path) -> Result<(EpicState, TaskId, String)>`, that performs load, root-id lookup, and root-task retrieval with uniform error messages. |
| 3 | note | `work_dir` (line 31) is used only to derive `state_path` and to `create_dir_all`. It is not a stale remnant -- it is live code. No action needed. | `src/main.rs:31` | -- |
| 4 | note | `cli.rs` is compact (31 lines) and clean. No dead code, stale comments, unused attributes, or legacy remnants found. | `src/cli.rs` | -- |

## Notes
- Grep confirmed zero matches for `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `subprocess`, `dead_code`, `TODO`, `FIXME`, `HACK`, or `XXX` in either file.
- The `tokio::spawn` / `spawn_blocking` calls on lines 175 and 182 are standard async runtime usage, not subprocess remnants.
- The Flick integration in `main.rs` uses the library crate (`FlickAgent::new`), with no subprocess or `--flick-path` CLI flag remnants, consistent with the migration noted in commit 68c1533.
