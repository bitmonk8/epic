# U6-R4: agent/models / Dead code & cruft

## Summary

Both files are clean and compact. The only finding is a stale `#[allow(dead_code)]` annotation on `SiblingSummary` whose comment claims `id` is "used in prompt formatting and tests," but `id` is never actually read anywhere in the codebase.

## Findings

| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | minor | `#[allow(dead_code)]` on `SiblingSummary` with stale comment claiming `id` is used in prompt formatting -- but `prompts.rs:46` formats only `goal`/`outcome`/`discoveries`, never `id`. The field is written (in `orchestrator.rs:280,294` and a test) but never read. | `mod.rs:16` | Either remove the `id` field entirely or start including it in the sibling prompt section (e.g., `format!("- [{}] {} ({}){disc}", status, s.goal, s.id)`). If keeping the field for future use, update the comment to say "field reserved for future use." |
| 2 | note | `models` module is private (`mod models`) while its two functions are `pub const fn`. Visibility is correct since only sibling module `config_gen` uses them, but `pub` on the functions is slightly broader than needed. | `mod.rs:5`, `models.rs:6,15` | Consider `pub(super)` or `pub(crate)` on the functions to express narrower intent, or leave as-is since the module gate already restricts access. |

## Notes

- No TODO/FIXME/HACK/XXX markers found in either file.
- No remnants of ZeroClaw, serde_yaml, flick_path, work_dir, subprocess, or spawn patterns.
- The `models.rs` file is minimal (39 lines) with solid test coverage for both functions.
- The `mod.rs` trait design is clean: no default method bodies, no stale commented-out methods, no deprecated signatures.
