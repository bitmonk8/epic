# U5-R4: agent/tools / Dead code & cruft

## Summary

The file is clean overall -- no TODO/FIXME/HACK markers, no `allow` annotations, no ZeroClaw/YAML/`flick_path` remnants. Three minor issues were found: two unused `ToolGrant` flag variants and a stale naming reference in a doc comment.

## Findings

| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | minor | `ToolGrant::TASK` is defined but never referenced anywhere in the codebase; no tool definitions are gated on it and no call site sets it. | `tools.rs:27` | Remove the variant (or add a `#[cfg(test)]` usage note if it is planned for future work). |
| 2 | minor | `ToolGrant::WEB` is defined but never referenced anywhere in the codebase; same situation as `TASK`. | `tools.rs:28` | Remove the variant or document its intended use. |
| 3 | note | `FlickToolDef` doc comment says "suitable for inclusion in a Flick config file" -- this is a stale reference to the old Flick subprocess-based workflow. The struct is now used in `config_gen.rs` for in-memory config generation, not for writing Flick config files. | `tools.rs:51` | Rename to `ToolDef` and update the doc comment (e.g., "A tool definition for the LLM tool-use schema"). |
| 4 | note | Line 1 comment lists "TASK, WEB presets" alongside READ/WRITE/BASH, but no preset mapping exists for TASK or WEB in `phase_tools` or `tool_definitions`. | `tools.rs:1` | Update the comment to match actual implemented presets, or remove the aspirational names. |

## Notes

- Grep searches for `TODO`, `FIXME`, `HACK`, `XXX`, `dead_code`, `allow(`, `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, and `child process` returned no hits (case-insensitive).
- `spawn` and `spawn_blocking` appear at lines 366, 415, and 567 but are all legitimate runtime usage (tokio task spawning and process spawning for bash tool).
- `verify_ancestors_within_root` is only called from one site (line 502) but serves a real security purpose; not flagged as dead code.
