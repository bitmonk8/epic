# U11-R4: init / Dead code & cruft

## Summary
`src/init.rs` is clean. No dead code, stubs, stale comments, subprocess/ZeroClaw/YAML remnants, or stale allow annotations were found.

## Findings
| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|

## Notes
- Grep searched for TODO, FIXME, HACK, XXX, dead_code, allow(, zeroclaw, zclaw, serde_yaml, flick_path, work_dir, subprocess, spawn, and "child process" -- zero matches.
- The sole public function `run_init` is called from `src/main.rs:60`. All private helpers (`present_and_confirm`, `print_step`, `edit_step`, `prompt_custom_step`, `prompt_models`, `prompt_limits`, `read_line_checked`, `read_line_or_eof`, `read_line`, `read_line_or_default`) are used within the file.
- The `use std::fmt::Write as FmtWrite` import is required by the `write!` macro on line 48 (writing into a `String`).
- Explicit `drop(lines)` calls exist in several functions to release the stdin lock before the function re-acquires it or returns; these are intentional and necessary for correctness, not cruft.
- No subprocess invocations, ZeroClaw references, YAML remnants, or stale annotations are present.
