# U14-R4: git / Dead code & cruft

## Summary
`src/git.rs` is a single-line comment stub (`// Git operations: commit, rollback, diff.`) with no implementation. It is declared via `mod git;` in `main.rs` but never used anywhere in the codebase.

## Findings
| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | major | Entire module is an empty stub -- the file contains only a comment, no code | `src/git.rs:1` | Remove `src/git.rs` and the `mod git;` declaration in `src/main.rs:5`, or implement the planned git operations |
| 2 | minor | Stale comment promises functionality that was never implemented ("commit, rollback, diff") | `src/git.rs:1` | If the module is kept as a placeholder, update the comment to indicate it is a stub; otherwise delete |

## Notes
- Grep searches for `TODO`, `FIXME`, `HACK`, `XXX`, `dead_code`, `allow(`, `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, `spawn`, and `child process` returned zero matches -- the file is too minimal to contain any of these patterns.
- No other file in `src/` references `git::` or imports from this module, confirming it is fully dead code.
