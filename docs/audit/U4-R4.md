# U4-R4: agent/prompts / Dead code & cruft

## Summary

The file is clean. No dead code, stubs, stale comments, subprocess/ZeroClaw/YAML remnants, or stale allow annotations were found.

## Findings

| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|

## Notes

- Grep search for `TODO`, `FIXME`, `HACK`, `XXX`, `dead_code`, `allow(`, `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, `spawn`, and `child process` returned zero matches.
- All public functions (`build_assess`, `build_execute_leaf`, `build_design_and_decompose`, `build_fix_leaf`, `build_design_fix_subtasks`, `build_verify`, `build_checkpoint`, `build_design_recovery_subtasks`, `build_assess_recovery`) are prompt builders that follow a consistent pattern and each has corresponding test coverage in the `#[cfg(test)]` module.
- The helper `format_context` is private and used by every prompt builder, so it is not dead code.
- No stale comments or orphaned code paths detected.
