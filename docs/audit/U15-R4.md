# U15-R4: metrics / Dead code & cruft

## Summary
`src/metrics.rs` is a stub module containing only a single comment ("Token usage and cost tracking.") with no actual code. It is declared via `mod metrics;` in `main.rs` but never imported or used anywhere in the crate.

## Findings
| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | minor | Empty stub module -- file contains only a comment and no code, yet is declared as `mod metrics;` in `main.rs` | `src/metrics.rs:1`, `src/main.rs:7` | Remove the `mod metrics;` declaration and delete `src/metrics.rs`, or implement the promised "Token usage and cost tracking" functionality |
| 2 | note | Stale aspirational comment -- the comment "Token usage and cost tracking." describes functionality that was never implemented | `src/metrics.rs:1` | If the feature is still planned, convert to a `// TODO:` so it is discoverable; otherwise delete |

## Notes
- The grep scan for `TODO`, `FIXME`, `HACK`, `XXX`, `dead_code`, `allow(`, `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, `spawn`, and `child process` returned zero matches in this file.
- A separate, unrelated `src/tui/metrics.rs` exists and provides a functioning `MetricsWidget` for the TUI dashboard. That file is clean of the searched-for patterns and is not part of this audit unit.
