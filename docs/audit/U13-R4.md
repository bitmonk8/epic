# U13-R4: tui / Dead code & cruft

## Summary
The TUI module is clean of subprocess, ZeroClaw, YAML, and flick-path remnants. Three `#[allow(...)]` annotations suppress warnings for genuinely unused fields and justified lint exceptions, though two of the suppressed fields could be removed outright.

## Findings
| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | minor | `#[allow(dead_code)]` on `TuiTask` suppresses warnings for `parent_id` and `path` fields that are written but never read. The comment says "used when interactive controls are added" -- this is speculative future code. | `src/tui/task_tree.rs:8` | Remove `parent_id` and `path` fields (and their writes in `mod.rs:150,154,179`) until they are actually needed, or add a tracking issue reference to justify keeping them. |
| 2 | minor | `#[allow(dead_code)]` on `WorklogEntry.timestamp` -- the field is set in every constructor but never read anywhere. The comment says "Kept for structured log export" but no such export exists. | `src/tui/worklog.rs:8` | Remove the `timestamp` field until a log-export feature is implemented, or reference a tracking issue. |
| 3 | note | `#[allow(clippy::too_many_lines)]` on `handle_event` (217 lines). The justification comment is accurate -- the method is a single match over event variants and splitting it would add indirection without benefit. | `src/tui/mod.rs:131` | Acceptable as-is. No action needed. |
| 4 | note | `#[allow(clippy::needless_pass_by_value)]` on `WorklogEntry` impl. The justification is accurate -- callers always pass owned `String` values from `format!()`. | `src/tui/worklog.rs:14` | Acceptable as-is. No action needed. |

## Notes
- No `TODO`, `FIXME`, `HACK`, or `XXX` markers were found in any TUI file.
- No traces of `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, `spawn`, or `child process` were found.
- The two `minor` findings (items 1 and 2) represent dead fields kept behind allow-annotations with forward-looking comments but no linked tracking issues. They are low-risk but add noise; removing them keeps the codebase honest about what is actually used.
