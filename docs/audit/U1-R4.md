# U1-R4: orchestrator / Dead code & cruft

## Summary

The orchestrator module is clean. No dead code, stubs, stale comments, subprocess/ZeroClaw/YAML remnants, or TODO/FIXME markers were found. All `allow` annotations are justified and documented. The file is well-structured production code (~1170 lines) with comprehensive tests (~3340 lines).

## Findings

| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | note | Three `#[allow(clippy::cast_possible_truncation)]` annotations suppress `usize as u32` casts on `.count()` results; the values are bounded by small constants (max 9) making truncation impossible, but the casts are not self-evidently safe without the comments | `orchestrator.rs:531`, `orchestrator.rs:573`, `orchestrator.rs:784` | Consider using `u32::try_from(...).unwrap()` to eliminate the lint and the allow annotation, or keep as-is since the comments explain the bound |
| 2 | note | Four `#[allow(clippy::too_many_lines)]` annotations on `leaf_fix_loop` (153 lines), `branch_fix_loop` (101 lines), `execute_branch` (129 lines), and `attempt_recovery` (144 lines); all justified with inline rationale | `orchestrator.rs:518`, `orchestrator.rs:673`, `orchestrator.rs:890`, `orchestrator.rs:1024` | Acceptable; `branch_fix_loop` at 101 lines is borderline and could be brought under the limit with minor refactoring if desired |
| 3 | note | `#[allow(clippy::struct_field_names)]` on test-only `MockAgentService` (8 of 10 fields end in `_responses`) | `orchestrator.rs:1182` | Acceptable for test code; the naming convention is clear and intentional |

## Notes

- Grep searches for `TODO`, `FIXME`, `HACK`, `XXX`, `dead_code`, `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, `spawn`, and `child process` all returned zero matches.
- No `#[allow(dead_code)]` annotations exist anywhere in the file.
- The `evaluate_scope` function and `ScopeCheck` enum are non-public but used internally by `check_scope_circuit_breaker` and tested directly -- not dead code.
- All production methods on `Orchestrator` are reachable through `run()`.
- No stale comments referencing removed features or old architecture were found.
