# U7-R4: task / Dead code & cruft

## Summary

The task module is clean overall with no subprocess/ZeroClaw/YAML remnants. Three issues were found: a write-only field (`decomposition_rationale`), an `#[allow(dead_code)]` annotation masking a genuinely unread field on `RecoveryPlan`, and a second `#[allow(clippy::struct_field_names)]` on `Task` that may no longer be needed.

## Findings

| # | Severity | Finding | Location | Suggestion |
|---|----------|---------|----------|------------|
| 1 | minor | `decomposition_rationale` on `Task` is written (orchestrator.rs:909) and serialized but never read back by any code path -- it is a write-only field | `task/mod.rs:117` | Either read it somewhere (e.g. TUI display, event logging) or remove the field and the write site |
| 2 | minor | `#[allow(dead_code)]` on `RecoveryPlan` suppresses a warning for the `rationale` field, which is constructed via `TryFrom<RecoveryPlanWire>` and in tests but never read in production logic (only `full_redecomposition` and `subtasks` are consumed) | `task/mod.rs:91` | Log or emit `rationale` in the recovery path (e.g. in the `RecoveryPlanSelected` event), then remove the allow; or drop the field if the value is not needed |
| 3 | note | `#[allow(clippy::struct_field_names)]` on `Task` (line 100) -- the struct has 16 fields, none of which share a common prefix that would trigger the lint today; the annotation may be stale | `task/mod.rs:100` | Try removing the annotation and running `cargo clippy`; keep it only if the lint fires |
| 4 | note | `#[allow(clippy::struct_field_names)]` on `Magnitude` (line 69) is justified -- all three fields share the `max_lines_` prefix | `task/mod.rs:69` | No action needed |

## Notes

- No `TODO`, `FIXME`, `HACK`, `XXX` comments were found in any task module file.
- No references to `zeroclaw`, `zclaw`, `serde_yaml`, `flick_path`, `work_dir`, `subprocess`, `spawn`, or `child process` were found.
- `leaf.rs` and `verify.rs` are minimal (comment-only / small struct definitions) with no dead code.
- `assess.rs` and `branch.rs` are clean data-definition modules with no cruft.
